info:
  name: deleteReservation
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/reservations/{{reservationId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
