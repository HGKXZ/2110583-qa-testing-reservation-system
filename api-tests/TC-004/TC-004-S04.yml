info:
  name: TC-004-S04
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/restaurants?name=Mars Diner"
  params:
    - name: name
      value: Mars Diner
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        // ตรวจ status code
        test("Status 200 OK", () => {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
