info:
  name: TC-004-S02
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/restaurants?tag=Japanese"
  params:
    - name: tag
      value: Japanese
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        // ตรวจ status code
        test("Status 200 OK", () => {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
