info:
  name: TC-004-S01
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/restaurants?keyword=MK Gold"
  params:
    - name: keyword
      value: MK Gold
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        // ตรวจ status code
        test("Status 200 OK", () => {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
