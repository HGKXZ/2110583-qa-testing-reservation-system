info:
  name: BF-008-005
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/reservations/{{reservationId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
