info:
  name: TC-008-003
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/reservations/{{reservationId}}"
  body:
    type: json
    data: |
      {
        "guestCount": 3
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
