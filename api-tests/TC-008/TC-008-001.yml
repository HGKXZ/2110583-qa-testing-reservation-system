info:
  name: TC-008-001
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/reservations/{{reservationId}}"
  body:
    type: json
    data: |
      {
        "guestCount": 4
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
