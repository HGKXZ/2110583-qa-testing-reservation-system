info:
  name: TC-001-001
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |+
      {
        "name": "John Doe",
        "email": "tc001001@test.com",
        "password": "Pass1234",
        "phone": "0812345678"
      }

  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status 201 Created", () => {
          expect(res.getStatus()).to.equal(201);
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
