info:
  name: TC-001-012
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/auth/register"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |+
      {
        "name": "John Doe",
        "email": "tc001012@test.com",
        "password": "Pass1234",
        "phone": "0812345678901234"
      }

  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 400", function() {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
