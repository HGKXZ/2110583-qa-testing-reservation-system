info:
  name: BF-009-005 (2)
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{baseUrl}}/reservations/{{reservationId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
