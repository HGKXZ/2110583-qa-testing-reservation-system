info:
  name: TC-009-004
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{baseUrl}}/reservations/{{reservationId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
