info:
  name: TC-005-004
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/tables/available?date=2026-04-15&time=18:00&guestCount=10"
  params:
    - name: date
      value: 2026-04-15
      type: query
    - name: time
      value: 18:00
      type: query
    - name: guestCount
      value: "10"
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        bru.setEnvVar("reservationId", data.reservationId);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
