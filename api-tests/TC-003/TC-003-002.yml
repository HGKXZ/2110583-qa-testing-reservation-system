info:
  name: TC-003-002
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/tables"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: fake.eyJ1c2VySWQiOjIsInJvbGUiOiJTdGFmZiIsImV4cCI6MTc3NjA1NzkwMDU4M30=.token

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        bru.setEnvVar("reservationId", data.reservationId);
    - type: tests
      code: |-
        // ตรวจ status code
        test("Status 401 Unauthorized", () => {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
