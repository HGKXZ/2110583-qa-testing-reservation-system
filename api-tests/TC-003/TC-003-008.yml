info:
  name: TC-003-008
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/auth/reset-password"
  body:
    type: json
    data: |-
      {  
        "token" : "{{token}}",
        "newPassword" : "PasswordOnly",
        "confirmPassword" : "PasswordOnly"
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        if (data.resetToken){
          bru.setEnvVar("token", data.resetToken);
        }
    - type: tests
      code: |-
        // ตรวจ status code
        test("Status 200 OK", () => {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
