info:
  name: TC-002-006
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/auth/logout"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        // ตรวจ status code
        test("Login success", () => {
          expect(res.getStatus()).to.equal(200);
        });

        // ตรวจ token มีใน response
        test("Has token", () => {
          expect(res.getBody().token).to.be.a("string");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
